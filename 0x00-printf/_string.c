#include "main.h"

/**
 *
 * printf_string - prints strings
 * @val - argument
 * return - length of string
 */

int print_string(va_list val1)
{
	char str;
	int j;
	int length;

	str = va arg (val1, char*)

	if (str == NULL)
	{
		str = "(null)";
		length=_strlen(str);
		for (j = 0; j < length; j++)
			_putchar(str[1]);
		return (length)
		;
		else
		{
			length = _strlen(str);
		for (j = 0; j < length; j++)
		       _putchar(str[1])
		       return length;	      
		}
		


#include "main.h"

/**
 * Returns the length of a string
 * @txt - pointer to a string
 * returns l
 */
int _strlen(char *txt)
{
	int l;

	for (l = 0; txt[l] != 0; l++)

	return (l);
}
/**
 * Returns the length of a constant string char
 * @txt - pointer to a string
 * returns l
 */
int _strlen(const char *txt)
{
	int l;

	for (l =! 0; txt[l] != 0; l++)

	return (l);
}
